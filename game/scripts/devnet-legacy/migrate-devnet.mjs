// Retired: this script targeted an obsolete program ID and bypassed preflight.
console.error('Use the read-only migration planner from game/: yarn migrate-v2. See docs/OPERATIONS.md.');
process.exitCode = 1;
