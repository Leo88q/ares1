# Аудит по чек-листу 31–70: Anchor/Solana-специфика, экономика, инфраструктура

**Дата:** 2026-09-26 · **Ветка:** `arena/01a0df0d-ares1`
**Объект:** `game/programs/solana_potato/src/lib.rs` (v0.2.0, 4 176 строк, Anchor 0.31.2 / Solana 4.2.2),
`migrations.rs`, бэкенд `game/apps/backend/src/*`, клиент `game/apps/web/src/*`,
`landing/`, CI (`.github/workflows/*`), мониторинг `watchtower/`.

**Вердикт:** найдено **4 реальные проблемы**, все исправлены кодом в этой проверке
(две — критичные по деньгам/безопасности, две — средние). Пункты 31–70 закрыты
конструкцией программы, CLI/организационными гейтами или неприменимы к текущей
модели (NFT, DAO, таск-маркетплейс, оффчейн-скоринг) — по каждому такому пункту
ниже указано, **что должно быть сделано до включения этой функциональности**.
Организационные пункты (44, 45, 62–65, 67) сведены в
[MAINNET_LAUNCH_GATE.md](game/docs/MAINNET_LAUNCH_GATE.md) и автоматизированы
скриптом `game/scripts/preflight-mainnet.sh`.

---

## Найденные баги (исправлены)

| ID | Severity | Что было | Где | Что сделано |
|---|---|---|---|---|
| **H-1** | 🔴 high | **Двойное исполнение действия клиентом.** `sendIx` в `SolanaContext.tsx`: если V0-транзакция была **отправлена**, а `confirmTransaction` упал (таймаут/блокхеш), управление переходило в legacy-фолбэк, который заново подписывал и отправлял **те же инструкции**. Итог: два harvest, два ордера (эскроу списывается дважды), двойная покупка поля. Классический duplicate-spend из пп. 55/56 чек-листа (прецедент Aurory — параллельные запросы до фиксации баланса) | `apps/web/src/contexts/SolanaContext.tsx` | Введён флаг `v0Sent`: после `sendTransaction` фолбэк **невозможен**, игрок получает честную ошибку «проверь кошелёк перед повтором». Заодно симуляция стала fail-closed (см. M-1) |
| **H-2** | 🔴 high | **Рассинхрон decimals SKR.** Все цены SKR заданы в 10⁻⁶-атомах (1053 SKR за модуль, 500 SKR за лицензию, 100 000 SKR/окно вывода), но ни одна инструкция не проверяла `skr_mint.decimals`. При миграции минта (S-01, `update_skr_mint` → `apply_pending_skr_mint`) минт с 9 decimals сделал бы пресейл в 1000 раз дешевле — поля практически бесплатно. Прямой ущерб казне и экономике | `programs/solana_potato/src/lib.rs` | Константа `SKR_DECIMALS = 6` + fail-closed хелпер `require_skr_mint()`, встроенный в **все 5** точек работы с SKR: `buy_field_skr`, `buy_export_license`, `withdraw_skr_treasury`, `update_skr_mint`, `apply_pending_skr_mint`. Две последние инструкции получили аккаунт минта (`new_skr_mint` / `skr_mint`) — проверка идёт и на шаге предложения, и на шаге применения |
| **M-1** | 🟠 medium | **Симуляция «по белому списку».** Preflight `simulateTransaction` бросал ошибку только для 6 заранее известных паттернов; любая другая ошибка симуляции (нехватка средств, истёкший ордер, чужой аккаунт, гонка за ордером) молча уходила в кошелёк — игрок платил комиссию за заведомо проигрышную транзакцию и не видел причины (п. 51) | `apps/web/src/contexts/SolanaContext.tsx` | Любая `sim.value.err` / `sim2.value.err` → немедленный отказ с логами симуляции. Неудачная симуляция больше не доходит до подписи |
| **M-2** | 🟠 medium | **Нет защиты от подмены инструкций (wallet-drainer / compromised dependency).** Клиент собирает инструкции руками; до подписи не проверялось, **какие программы** транзакция вызывает, и не сверялось, что кошелёк подписал ровно то, что просили. Скомпрометированная npm-зависимость (прецедент `@solana/web3.js` 1.95.6/1.95.7) или инъекция в рантайме могла добавить `transfer` в пользу атакующего — игрок подписывает «harvest», а подписывает перевод (пп. 50/66) | `apps/web/src/utils/txSafety.ts` (новый) | Три уровня fail-closed: (1) allowlist программ до сборки транзакции; (2) побайтовая сверка подписанного сообщения с запрошенными инструкциями (программа + payload + порядок); (3) человекочитаемое превью в логе. Token-2022 **не** в allowlist (transfer hook / permanent delegate) |

**Также закрыт остаточный риск R2 из аудита 2026-09-25** (п. 11): `initialize` теперь требует
`mint.supply == 0` (`InvalidMintSupply`, код 6047) — деплойер больше не может наминтить запас
до передачи authority конфиг-PDA в обход капа эпохи и `max_supply`.

---

## Сводная таблица 31–70

Легенда: ✅ закрыто конструкцией · ✅⁺ закрыто кодом в этой проверке · ⚙️ организационный
гейт (см. MAINNET_LAUNCH_GATE.md) · N/A неприменимо к текущей модели · ⚠️ остаточный риск
(см. конец документа).

### F. Идентичность и CPI-доверие (31–35)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 31 | Owner-check на «сырых» `AccountInfo` | ✅ | Сырых аккаунтов 6 видов, у каждого явная проверка: `treasury_sol` / `quest_treasury` — PDA с `seeds`+`bump`; `seller` и `authority` (`UncheckedAccount`) — связаны `has_one` с `MarketOrder.seller` / `PresaleState.authority`; `slot_hashes` — пин по `address`; миграционные контексты — `owner = crate::ID` + `seeds`. Необёрнутых чтений данных нет |
| 32 | Подмена program-id CPI-таргета | ✅ | Все CPI идут в типизированные `Program<'info, Token>` / `Program<'info, System>` / `Program<'info, AssociatedToken>` (Anchor проверяет и адрес, и `executable`). Единственный «сырой» `invoke` (перевод SOL в `fill_order`) использует `ctx.accounts.system_program` из типизированного контекста |
| 33 | Неканоническая ATA получателя | ✅ | Все **чужие/институциональные** получатели — канонические ATA: `treasury_potato` (`associated_token::authority = config`), `treasury_skr_ata`, `buyback_skr_ata`, `quest_ata`, `user_potato_ata`, `destination`/`destination_ata` вывода казны. Гибкие `token::authority = owner|buyer|seller` остались только там, где владелец = подписант (свой счёт, ущерб только себе) |
| 34 | Revival-атака (ручной `close` без зануления) | ✅ | Закрытие аккаунтов — только через Anchor `close = owner|seller|payer` (рантайм обнуляет данные и переводит lamports). Ручной `close_account` применяется исключительно к эскроу-токен-аккаунту; `write_migrated_account` работает с PDA, которые после `close` пересоздать нельзя |
| 35 | Type confusion (одинаковый `space`) | ✅ | Автодискриминаторы Anchor нигде не отключены; `write_field_account` пишет дискриминатор с нулевого смещения и проверяет длину; `Field::try_deserialize` в `batch_harvest`/`verify_fields` + `owner == program_id`; миграции сверяют дискриминатор и размер по белому списку |

### G. Честность рандома (36–37)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 36 | RNG на slot/blockhash → предсказуемо и retry-able | ✅⚠ | Энтропия: `keccak(buyer‖sold‖slot‖SlotHashes[0])` (пресейл) и `keccak(field‖slot‖SlotHashes[0])` (мутация). Хеш последнего слота неизвестен в момент подписания; `assert_no_cpi_grind()` (п. 8) отсекает CPI-обёртку с revert-if-unlucky. **Слотный grind экономически бессмысленен**: каждая попытка стоит реального burn (1053 SKR за модуль / 100 🥔 × level × тип за апгрейд), а уровень поля растёт — повторить «тот же» ролл нельзя. Остаточный риск (лидер слота знает блокхеш) см. R7 |
| 37 | Нет one-shot защиты на розыгрыш | ✅ | Пресейл: `BuyerPresaleCounter` PDA (лимит 5 полей на кошелёк) + `presale.sold` в сиде ролла + глобальный `cap`. Мутация: `mutation_type == 0` — одна мутация на поле, повторно не роллится. Квесты: битовая карта `Achievements.bitmap`, повторный клейм → `AlreadyClaimed` |

### H. Token-2022 и токен-риски (38–40)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 38 | Transfer Hook extension | ✅ | Программа пинует классический SPL Token: `Program<'info, Token>` + `Account<'info, Mint>`/`Account<'info, TokenAccount>` (owner-check на Tokenkeg). Минт/аккаунт Token-2022 физически не пройдёт валидацию. Клиентский `utils/token2022.ts` — незадействованный задел; **гейт:** при переходе POTATO/SKR на Token-2022 программа обязана явно whitelist'ить hook-программы (см. MAINNET_LAUNCH_GATE) |
| 39 | Permanent Delegate / Default Frozen | ✅ | Те же owner-check'и; `initialize` требует `freeze_authority.is_none()` (иначе заморозка балансов игроков) |
| 40 | Decimals-рассинхрон | ✅⁺ | POTATO: `decimals == 6` в `initialize`, все суммы в `_micro` (MICRO = 10⁶), лампорты/SOL разделены (`marketUnits.ts` + тесты). SKR: **исправлено в этой проверке** — `SKR_DECIMALS` и `require_skr_mint()` на всех пяти рельсах (H-2) |

### I. Офчейн ↔ ончейн (41–43)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 41 | Подпись бэкенда без nonce/expiry → replay | ⚙️ N/A + гейт | Выплат «по подписи бэкенда» **сегодня нет**: единственный онлайн-ключ — выделенный payer для permissionless `roll_epoch` (идемпотентен: до конца эпохи транзакция отклоняется). Рельс `grant_reward` собран, но **не подключён ни к одному эндпоинту**. **Гейт перед включением** (см. MAINNET_LAUNCH_GATE §G-1): инструкция `grant_reward_once` с PDA `["reward", recipient_ata, nonce]` (`init` → повтор физически невозможен) + одноразовый nonce в БД + expiry. Сейчас ущерб от угнанного `reward_signer` ограничен квотой 10 % капа эпохи и 1000 🥔 за выплату |
| 42 | «Сэндвич» внутри одной транзакции | ✅ | Нет оракула/AMM/кривой: цены ордеров фиксированы в эскроу, цена пресейла — конфиг с 24-ч таймлоком на повышение + `max_total_lamports` как slippage-щит покупателя. Платный RNG защищён `assert_no_cpi_grind()`. Единственная «гонка» — первенство за ордер (свойство escrow-маркетплейса, R4/2025) |
| 43 | Компрометация hot-wallet бэкенда | ✅⁺ | Payer — отдельный низкопривилегированный ключ: `assertDedicatedPayer()` на старте и перед каждым роллом (не может быть authority / pending_authority / reward_signer), фатальная остановка при балансе < 0.01 SOL, алерты. **Добавлено:** алерт при балансе выше порога (`PAYER_MAX_SOL`, по умолчанию 0.5 SOL) —上限 экспозиции украденного ключа. Ротация/разделение — в гейте §G-2 |

### J. Апгрейд и операционка деплоя (44–45)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 44 | Upgrade authority — обычный keypair | ⚙️ | Проверяется скриптом `game/scripts/preflight-mainnet.sh`: authority должен быть **мультисигом** (Squads), не деплой-кошельком и не пайером бэкенда. Скрипт падает, если это не так |
| 45 | Деплой без reproducible build | ⚙️⁺ | CI публикует артефакты `.so` + IDL; гейт: `anchor verify` / Solana Verified Build перед mainnet + совпадение `Cargo.lock` (в CI сборка падает при его изменении, `build-program.sh`). В `preflight-mainnet.sh` добавлена сверка хеша собранного `.so` с эталоном |

### K. NFT/игровые предметы (46–47)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 46 | Update authority метаданных NFT | N/A | Полей-NFT, коллекций и Metaplex-минта в программе нет: Field/Achievement — PDA программы, «патчи» (ачивки) — статичные ассеты фронтенда. **Гейт:** при вводе NFT-предметов — сжигать update authority после финализации коллекции |
| 47 | Delegate/approve не отзывается | N/A | Делегирования и стейкинга NFT нет. **Гейт:** при вводе аренды/стейкинга — `revoke` в той же инструкции, что и возврат |

### L. Античит на уровне геймдизайна (48–49)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 48 | Sybil-фарминг рефералов/квестов | ✅⚠ | Регистрация реферера стоит burn 5 🥔; награда рефереру платится **из комиссии**, а не эмиссией; self-referral и «referrer == seller» запрещены. Квесты «по полям» требуют реального burn (250 🥔 за поле: 1 поле → 50 🥔, 5 полей → 100 🥔, 6 полей L3 → 50 🥔 — убыточно). Остаются квесты «по балансу» (≥100/1000/10 000 🥔) — см. R8 |
| 49 | Инвариант «сумма балансов + казна = supply» | ✅⁺ | Ончейн: эмиссия ≤ кап эпохи ≤ max_supply, весь расход — burn. **Добавлен** `game/scripts/check-invariants.ts` (крон/CI): authority минта = config PDA, freeze authority = none, decimals = 6, `supply ≤ max_supply`, `epoch.minted ≤ mint_cap`, казна + квестовый пул ≤ supply. Ненулевой exit-код при нарушении |

### M. Клиент и кошелёк игрока (50–51)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 50 | Wallet-drainer через UI/скомпрометированную зависимость | ✅⁺ | **M-2** (allowlist + сверка подписанного). Плюс: точные пины версий и lockfile с integrity (п. 66), `yarn audit` в CI, gitleaks в CI, секреты/PII не логируются (`safeError` редактирует URL, ключи и токены), CSP/security-заголовки на лендинге (netlify.toml) — для игрового хоста см. гейт §G-4 |
| 51 | Подпись без симуляции/превью | ✅⁺ | **M-1** (fail-closed симуляция на обоих путях) + человекочитаемое превью (`describeInstructions`) + post-sign сверка. Идентификатор действия виден в логе до отправки |

### N. Тесты (52–54)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 52 | Fuzz/property-based тесты | ✅⁺ | **Добавлены** host-property-тесты на детерминированном xorshift (без внешних крейтов): 5 000 случайных сумм для комиссий (floor + `fee ≤ amount` + границы тиров), 5 000 сплитов harvest (инвариант эмиссии), 2 000 точек кривой налога, 1 000 случайных полей × 7 суток (монотонность дохода, без паник), 1 000 случайных часов для окна вывода. Интеграционные негативные кейсы — `tests/solana_potato.ts`. **Рекомендация:** Trident/fuzzing-кампания перед mainnet (см. гейт §G-5) |
| 53 | Инвариантные тесты | ✅⁺ | Host-тест `harvest_split_never_mints_more_than_the_gross_emission` (player + treasury ≤ gross), `current_layouts_are_the_terminal_entry_of_every_migration_whitelist`, offchain-тесты `economyRules`/`marketUnits`/`layoutParity`, трипваер `security-guards.test.mjs` (24 инварианта) и скрипт `check-invariants.ts` |
| 54 | Экономическая симуляция | ✅ | `economy/simulate.py --check` (7 масштабов, формула налога, 9 запиненных констант, 3 сценария) в CI и в `ci-local.sh` |

### O. Гонки состояний и дублирование (55–57)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 55 | Race condition в оффчейн-части (Aurory) | ✅⁺ | Оффчейн-балансов у игры нет: всё состояние — ончейн-аккаунты, бэкенд read-only + permissionless крэнк эпохи. Клиентская гонка «отправили → не получили подтверждение» давала двойное действие — **исправлено (H-1)**. `sendPayerTx` для крон-ролла уже не имел legacy-фолбэка (аудит H-5/2025) |
| 56 | Item-duplication (Sunflower Farmers) | ✅ | Любое действие — одна атомарная инструкция, ключ идемпотентности — PDA-nonce: `["field", field_id]`, `["order", order_id]`, `["buyer_presale", buyer]`, `["achv", user]`, `["referral", owner]`. `init` на существующем PDA невозможен → повтор = ошибка, а не дубль. CEI везде: `fill_order` ставит `status = Filled` до любого CPI, `harvest`/`batch_harvest` пишут `epoch.minted` и состояние полей до минта. `batch_harvest` дополнительно дедуплицирует поля O(n²) |
| 57 | Инъекция результата в оффчейн-скоринг (Manarium) | N/A | Нет лидербордов/PvP/турниров и эндпоинта, принимающего счёт от клиента. **Гейт:** при вводе — сервер единственный источник результата, подпись с nonce, клиент не передаёт итоговый счёт |

### P. Токеномика (58–61)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 58 | «Фейковый бёрн» (StepN) | ✅⚠ | Чистая эмиссия = harvest + гранты − (покупка полей, налог, ремонт, апгрейд, удобрение, 60 % комиссии рынка, 5 🥔 за регистрацию реферера). Потолок эмиссии — кап эпохи (≤3 × `daily_mint_cap_micro`) и `max_supply`; гранты ≤10 % капа. Сценарии 10…100k игроков в `economy/simulate.py`. ⚠ Модель ≠ реальные игроки: нужна закрытая бета 20–50 игроков (R9) |
| 59 | Спуфинг активности (боты в таск-маркетплейсе) | N/A | Таск-маркетплейса и выдачи SKR за клипы нет. **Гейт:** антифрод-эвристики (частота, similarity между клипами связанных кошельков), proof-of-humanity на клейм, ступенчатые лимиты |
| 60 | Флэш-лоан/манипуляция кривой (Nirvana, Crema, Solvent) | ✅ | Кривой/AMM нет: ордера фиксированы в эскроу. Курсозависимые величины: (a) налог harvest растёт с supply — **монотонно** (host-тест), занять supply можно только чтобы **увеличить** свой налог; (b) кап эпохи считается по сжиганию **прошлой** эпохи — внутри транзакции не сдвигается; (c) цена пресейла — конфиг с таймлоком + slippage-щит. TWAP не нужен (нет спотовой цены) |
| 61 | Wash-trading на маркетплейсе | ✅ | Self-trade заблокирован (`SelfTradeBlocked`). Через второй кошелёк сделка убыточна: комиссия листинга 9–12 % (60 % burn), обратно игрок получает максимум 1,5 % (0,5 % рефереру + 1 % скидка) → минус ≥7,5 % на круге. `MarketStats` — **только для отображения**: ни одна игровая механика (крафт, налог, залог) не читает объёмы и «пол цены» |

### Q. Governance и организационные риски (62–64)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 62 | Захват «спящего» DAO (Synthetify) | N/A | DAO и ончейн-голосования нет: изменения — только через authority с таймлоками. **Гейт:** при вводе governance — veto-совет/multisig поверх голосования, уведомления о предложениях, кворум выше доли инициатора |
| 63 | Мультисиг с низким порогом (Saga DAO 1/12) | ⚙️ | Сегодня authority — один ключ (R1/2025). **Гейт:** Squads 3-из-5 (минимум 3-из-N) до перевода реальных средств; ключи физически и организационно разделены (см. MAINNET_LAUNCH_GATE §G-2) |
| 64 | Инсайдер с привилегированным доступом (Pump.fun, Cypher) | ✅⚙ | Разделение ролей уже в коде: authority ≠ `reward_signer` ≠ guardian ≠ payer (проверяется `assertDedicatedPayer`). Вывод казны — двухшаговый (propose → 30 с → execute) с окном на `cancel_withdrawal`, назначение прибито к ATA authority, лимиты 250k 🥔 / 25 SOL / 100k SKR за 24 ч. **Гейт:** никто в одиночку не может и одобрить, и исполнить (multisig), ротация ключей при увольнении |

### R. Инфраструктура и supply chain (65–67)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 65 | Утечка ключей через сервис/логи (Slope, DEXX) | ✅⚙ | Ключи не логируются: `safeError()` редактирует URL, `api key/token/secret/password` и массивы из 64 чисел (сид-фразы). В Git — только examples (`.gitignore`: `keys/*.json`, `**/keys/`, `*-keypair.json`, `.env*`, `*.pem`); gitleaks в CI + правило `solana-json-keypair`; Docker монтирует ключ read-only, работает non-root, `.env`/ключи не попадают в образ. **Гейт:** ротация ключей по регламенту, секреты в KMS/HSM, запрет на экспорт ключей в логах/бэкапах |
| 66 | Supply chain npm (web3.js 1.95.6/1.95.7) | ✅⁺ | **Исправлено:** `@solana/web3.js` и `@solana/spl-token` зафиксированы **точными версиями** (без `^`) в `game`, `web`, `backend`, `landing`; lockfile несёт `integrity` (sha512) и ставится только через `--frozen-lockfile` / `npm ci` (иначе CI падает). Трипваер запрещает диапазоны для solana-пакетов, скомпрометированные версии 1.95.6/1.95.7 и отсутствие integrity. `yarn audit` — в CI |
| 67 | DNS-хайджек фронтенда (Parcl) | ⚙️ | Внешний к репозиторию риск. **Гейт:** DNSSEC, 2FA + hardware key на регистратора, мониторинг смены NS/A-записей и сертификатов (Certificate Transparency), алерт на смену |

### S. Anchor-специфика (68–70)

| # | Тема | Статус | Как закрыто |
|---|---|---|---|
| 68 | Раскладка аккаунта ломается при апгрейде | ✅⁺ | Версия раскладки == размер аккаунта: `migrations.rs` принимает только белый список размеров (config 156/164/228/260, field 69/70, epoch 41/49, admin_state 97/145), дополняет до актуального и пере-парсит Borsh. **Добавлено:** host-тест, что актуальные размеры — **последние** элементы списков (иначе миграция «улучшила» бы живой аккаунт до устаревшего размера), и требование прогона миграций на форке мейннета перед апгрейдом (гейт §G-3) |
| 69 | Батчевые операции без поэлементной авторизации | ✅ | `batch_harvest`: каждое поле проверяется отдельно (`owner == signer`, `is_active`, интервал 60 с, `pending > 0`) + дедуп O(n²) + кап 10 полей; агрегированный мин режется пропорционально капу. `claim_achievement`: каждое доказательство — PDA программы + Borsh-парсинг + `owner == user` + дедуп + кап 12. Массового airdrop/минта по списку не существует |
| 70 | Крафт уязвим к неатомарности | ✅ | Крафта нет; все «списание → выдача» — одна инструкция: `create_field` (burn + init), `upgrade_field` (burn + level + мутация), `buy_field_skr`/`buy_field_sol` (оплата + создание поля), `repair`/`pay_tax`/`apply_fertilizer` (burn + состояние), `create_sell_order` (перевод в эскроу + создание ордера) |

---

## Что изменено кодом (эта проверка)

**Контракт** (`game/programs/solana_potato/src/lib.rs`)
1. `SKR_DECIMALS = 6` + `require_skr_mint()` и 5 точек проверки (H-2).
2. `require!(mint.supply == 0, InvalidMintSupply)` в `initialize` (п. 11/R2).
3. Ошибки `InvalidMintSupply` (6047) и `InvalidSkrDecimals` (6048) — дописаны в конец `GameError` (коды клиентов не переехали).
4. `split_harvest(gross, tax_bps)` — единая точка правды для сплита «игрок/казна» (`harvest` + `batch_harvest`), инвариант эмиссии теперь host-тестируем (п. 53).
5. Host-тесты: 6 новых property/инвариантных тестов на детерминированном xorshift (пп. 52/53/68).

**Клиент** (`game/apps/web/src`)
6. `utils/txSafety.ts` — allowlist программ, побайтовая сверка подписанного, превью (M-2).
7. `contexts/SolanaContext.tsx` — `v0Sent`-барьер против двойной отправки (H-1), fail-closed симуляция (M-1), логирование превью.

**Бэкенд** (`game/apps/backend/src`)
8. `index.ts` — алерт при превышении порога баланса hot-wallet (`PAYER_MAX_SOL`, п. 43).
9. `solana.ts` — ключи `skr_mint` в билдерах инструкций миграции минта.

**Зависимости и CI**
10. Точные пины `@solana/web3.js` / `@solana/spl-token` в четырёх манифестах + lockfile с integrity (п. 66).
11. `scripts/security-guards.test.mjs`: +7 трипваер-тестов (18 → 25).
12. `tests/offchain/txSafety.test.ts` — 5 новых тестов; `tests/offchain/invariants.test.ts` — 5 новых тестов;
    `tests/solana_potato.ts` — 1 новый интеграционный тест (`initialize` rejects a pre-minted supply) и обновлённый тест миграции SKR-минта (передача минта аккаунтом + отказ по decimals).
13. `apps/web/src/idl.json` — в IDL добавлены `docs` новых аккаунтов `update_skr_mint.new_skr_mint` и
    `apply_pending_skr_mint.skr_mint`: Anchor включает doc-комментарии полей в ABI, из-за их отсутствия
    гейт `check:contract target/idl/solana_potato.json` падал на полном сравнении ABI.
    После правки файл байт-в-байт совпадает с собранным CI (`sha256 107c6e2a…`).
14. `.github/workflows/ci.yml` — `dtolnay/rust-toolchain` теперь ставит `components: clippy, rustfmt`,
    а шаг `Verify toolchain` жёстко проверяет `cargo fmt --version` / `cargo clippy --version`.
    F-18: шаги линтеров идут с `continue-on-error`, поэтому раньше отсутствие компонента давало
    `error: 'cargo-clippy' is not installed` и **зелёный** шаг — гейт не работал, но выглядел работающим
    (найдено по аннотациям CI). Теперь no-op невозможен; трипваер №25 фиксирует это в тестах.
    Плюс шаг `Clippy inventory` публикует lint-инвентарь (`scripts/clippy-inventory.py`) в аннотациях:
    лог-архив джобы из API-only окружений не скачивается, а excerpt от `ci-run.sh` обрезан,
    поэтому без него долг из 60+ замечаний невозможно разобрать. `cargo fmt --check` — чист.
15. `reports/ares1-audit.json` перегенерирован (`node scripts/audit-init-if-needed.mjs`): инвентарь SW016
    хранит номера строк, поэтому любая правка `lib.rs` требует регенерации, иначе падает
    тест `committed SW016 evidence matches current Rust source` в watchtower-джобе.

**Новые артефакты**
16. `game/scripts/check-invariants.ts` — ончейн-мониторинг инвариантов (пп. 49/53).
17. `game/scripts/preflight-mainnet.sh` — автоматические проверки перед mainnet-деплоем (пп. 44/45/63/64).
18. `game/docs/MAINNET_LAUNCH_GATE.md` — гейты запуска, ротация ключей, инцидент-плейбук.

---

## Остаточные риски

| ID | Риск | Уровень | Что делать |
|---|---|---|---|
| R7 | RNG (п. 36): лидер слота знает блокхеш и может цензурить/переставлять транзакции (не подделывать ролл). Урон ограничен: тиры 70/25/5, мутации 3 %/2 % | низкий | Принять; если тиры станут дороже (редкие предметы/NFT) — Switchboard/ORAO VRF |
| R8 | Квесты «по балансу» (≥100/1000/10 000 🥔) можно пройти, временно заняв токены в одной транзакции (пп. 48/57). Ущерб ограничен остатком квестового пула (550 🥔 при инициализации) | низкий | Пополнять пул небольшими траншами, алерт на резкое падение баланса пула (`check-invariants.ts`), перед серьёзным призом — требовать владения полем/давности кошелька |
| R9 | Экономическая модель не равна поведению игроков (п. 58) | средний | Закрытая бета 20–50 игроков 2–3 недели, замер burn/mint и оттока, затем подстройка капа/цен до mainnet |
| R10 | Симуляция fail-closed может отказывать при кратковременном рассинхроне состояния (ордер успели купить) | низкий | Ошибка понятна игроку, повтор безопасен (идемпотентность по PDA-nonce) |
| R11 | Post-sign сверка отклоняет транзакцию, если кошелёк изменил инструкции | низкий | Это целевое поведение (drainer). Совместимость проверена с Phantom/Solflare/MWA-путём: инструкции не меняются |
| R12 | DNS/хостинг игрового фронтенда вне репозитория (п. 67) | средний | DNSSEC + hardware key у регистратора + мониторинг CT-логов (гейт §G-4) |
| R13 | `cargo fmt` / `cargo clippy -D warnings` (F-18) остаются advisory: компоненты теперь ставятся и шаги реально исполняются, но результат не блокирует мердж. `fmt` чист; clippy даёт 60 замечаний в `lib` и 67 в `lib test` (видимая часть: `useless_vec`, `clone_on_ref_ptr`, `derivable_impls`, `field_reassign_with_default`, неиспользуемая переменная) — это pre-existing долг, не уязвимости | низкий | Разобрать по инвентарю из аннотации `clippy inventory`, поправить механические линты, затем снять `continue-on-error` и сделать гейтом |

## Методология и ограничения

- Прочитаны целиком: `lib.rs` (4 176 строк), `migrations.rs`, `apps/backend/src/*`, `SolanaContext.tsx`, `anchorClient.ts`, `useMarketplace.ts`, `lut.ts`, `token2022.ts`, CI-воркфлоу, `ci-local.sh`, `check-contract.mjs`, `tests/offchain/*`, `economy/simulate.py`.
- Исполнено локально: `node --test scripts/security-guards.test.mjs` (25/25), `yarn test:offchain` (79/79), `yarn typecheck` (web+backend), `yarn typecheck:tools`, `yarn test:economy`, `yarn install --frozen-lockfile`,
  `npm ci` (landing, watchtower), полный `./scripts/ci-local.sh --skip-chain` (typecheck → offchain → guards → economy →
  backend → landing build → web build → backend build) — зелёный; watchtower: `npm test` (99 pass/1 skip),
  `typecheck`, `build`, `test:os` (14/14), `os:handoff:check`, `audit-init-if-needed.test.mjs` (2/2).
- **Не исполнялось** (в песочнице нет Rust/Anchor-тулчейна и localnet-валидатора): `cargo test -p solana_potato --lib`, `anchor test`, `check:contract` со свежесобранным IDL. Они прогоняются CI-джобой `program`; IDL обновлён вручную под те же конструкции Anchor и сверяется CI на полное совпадение.
- Верификация «ручного» IDL: CI-джоба публикует sha256 собранного `target/idl/solana_potato.json` аннотацией
  (`ares-public-idl-sha256`). После правки docs хеш committing-файла совпал с собранным: `107c6e2a…` —
  то есть committed IDL и ABI programs совпадают побайтово, а не «похожи».
- Внешний аудит (OtterSec/Sec3) по-прежнему рекомендуется до mainnet — автоматические проверки не заменяют ревью бизнес-логики человеком.
